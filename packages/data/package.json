{
    "name": "@optics/data",
    "version": "0.3.2",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist/**/*"
    ],
    "scripts": {
        "build": "tsc --skipLibCheck",
        "clean": "rm -rf dist && rm tsconfig.tsbuildinfo",
        "rebuild": "pnpm clean; pnpm build"
    },
    "dependencies": {
        "@optics/core": "workspace:*"
    }
}
